apiVersion: v1
kind: Service
metadata:
  name: {{ include "w9s.fullname" . }}
  labels:
    {{- include "w9s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "w9s.selectorLabels" . | nindent 4 }}
